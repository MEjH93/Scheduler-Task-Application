#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../a/node_modules/a_test/dist/bin/a.js" "$@"
  ret=$?
else 
  node  "$basedir/../a/node_modules/a_test/dist/bin/a.js" "$@"
  ret=$?
fi
exit $ret
